# frozen_string_literal: true

source 'https://rubygems.org'

ruby '2.5.1'

group :development do
  # Static code analysis
  gem 'rubocop', '~> 0.58.0'
end

group :test do
  # Build tool for running tests
  gem 'rake', '~> 12.3.1'
  # BDD test framework
  gem 'rspec', '~> 3.7.0'
  # Allow debug in RubyMine IDE
  gem 'ruby-debug-ide', '~> 0.6.1'
end
